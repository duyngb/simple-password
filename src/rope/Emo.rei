/** A poor man's implementation of Unicode emoji finder.
 *
 * Given a list of codepoint, this function returns list of found
 * emojis in that string.
 */
let emoji_folder: list(int) => list(list(int));
